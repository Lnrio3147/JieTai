# 01 · 双目模型对比

目标：引入 LiteAnyStereo，并与原 IGEV 模型进行统一输入、统一参考和统一指标的对比。

- `rec_img_set/`：203 场正式实验，其中 FDJYP-3 的 73 场用于定量评价。
- `jop1/`：Jop1 全部 9 场对比。
- `las_training/`：LiteAnyStereo 的 JMP 训练运行，属于支撑记录，不与正式对比结果混用。

正式结果优先看 `rec_img_set/results/final_203/` 和 `jop1/results/final_9/`。
