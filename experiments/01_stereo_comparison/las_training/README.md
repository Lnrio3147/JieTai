# LiteAnyStereo 训练记录

`results/run_v1/` 保存 JMP-LF6020 上的训练 checkpoint、日志和验证汇总。训练代码位于 `../../../projects/LiteAnyStereo/`，训练数据位于 `../../../datasets/training/JMP-LF6020-ETH3D/`。

项目内的 `runs/training/jmp_lf6020_las1` 是兼容入口。该训练运行属于支撑实验，不替代 `../rec_img_set/results/final_203/` 的正式模型对比结论。
