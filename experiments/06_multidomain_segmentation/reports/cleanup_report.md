# 实验 6 清理记录

清理日期：2026-08-21。

目录由约 `1.6 GB` 缩减到约 `215 MB`。删除对象使用系统回收站，清空回收站前仍可恢复。

## 已清理

- 已淘汰训练权重：多域 V1、tune02～tune05；
- 重复 checkpoint：两个保留模型的 `latest.ckpt`，保留各自 `best.ckpt`；
- 重复推理缓存：旧模型、V1、V2、V3和参数搜索产生的概率数组、掩码及对比图；
- 已被 V4.1 取代的 V4 入口；
- Python `__pycache__`。

## 保留原则

- 保留最终召回路线 V4.1及其真实后端权重；
- 保留综合质量路线 balanced V2；
- 保留两条路线的最终可视化和完整指标；
- 保留全部实验报告，历史参数和结论不会随大文件一起丢失。

新建的RGB-D双流模型不属于实验6，已迁移到 `experiments/07_rgbd_fusion/`。
